/**
 * Global application constants.
 *
 * These values should remain static across the application.
 * Environment-specific values belong in config/, not here.
 */

export const APP_NAME = "Link2Stream Uploader";

export const APP_VERSION = "0.1.0";

export const API_PREFIX = "/api";

export const HEALTH_ENDPOINT = "/health";

export const DEFAULT_PORT = 3000;

export const DEFAULT_TIMEZONE = "UTC";

export const DEFAULT_LOG_LEVEL = "INFO";

export const JOB_ID_PREFIX = "JOB";

export const DOWNLOAD_ID_PREFIX = "DL";

export const ENCODE_ID_PREFIX = "ENC";

export const UPLOAD_ID_PREFIX = "UP";

export const PREVIEW_ID_PREFIX = "PRE";

export const INVENTORY_ID_PREFIX = "INV";